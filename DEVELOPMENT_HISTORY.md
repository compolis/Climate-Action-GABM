# Development History


## Table of Contents
- [Overview](#overview)
- [GitHub Copilot](#github-copilot)
- [Contributors](#contributors)
- [Development Timeline](#development-timeline)


## Overview
This document summarises the development of Climate-Action-GABM, including key decisions, milestones, and collaborative experiences. A summary of changes is provided in the [Change Log](CHANGE_LOG.md).


## GitHub Copilot
[GitHub Copilot](https://github.com/features/copilot) has been used to help automate release and cleanup tasks, develop document, test and formulate code.


## Contributors
See [CONTRIBUTORS].


## Development Timeline
- [2026-07-10] v0.9 — Degree/centrality-proportional peer fan-out (`k_i ∝ network measure`). Peer chat (the `C` phase) was a flat *send* fan-out: every citizen relayed its reflection to the same `k = min(k_peers, degree)` randomly-chosen neighbours, so a 40-degree hub spread its shifted view no wider than a leaf with two ties — exactly the mechanism limitation the paper's targeting null calls out ("a targeted hub cannot pass its shifted opinion to more neighbours than anyone else … a degree-proportional relay rule … [is] the natural re-test"). New `SurveyedNation._compute_peer_fanout()` in [src/cag/abm/environment.py](src/cag/abm/environment.py) computes a per-citizen fan-out `k_i` scaled by a peer-graph measure — `degree` or `betweenness` (reusing the centrality machinery already built for reach targeting) — under two budget policies: `additive` (the default) sets `k_i = scale·measure_i` so the well-connected simply relay to more people and total relay volume grows with connectivity (the realistic posting-network reading), while `preserve` instead redistributes a fixed total budget so the *mean* fan-out stays `k_peers` (isolating placement from volume, so it stays comparable to the iso-impression result). All non-constant modes clip `k_i` into `[kmin, kmax]` (kmax caps runaway fan-out on high-degree hubs) and never exceed a node's own degree; the map depends only on the static peer graph, so it is memoised per parameter set. `mode="constant"` (the default) reproduces the historical rule byte-for-byte, so existing runs are unchanged. Wired through `run_peer_messaging` / `run_package_peer_messaging`, three exposed `SIM_CONFIG` keys (`peer_fanout_mode` / `_budget` / `_kmax`; budget defaults to `additive` and `kmax` to `10`, with `scale`=1.0 and `kmin`=1 baked as internal defaults) plus the `rt` runtime dict and both `C`-phase dispatch sites in [src/cag/abm/sim.py](src/cag/abm/sim.py), and `--peer-fanout-mode/-budget/-kmax` CLI flags in [src/cag/__main__.py](src/cag/__main__.py). `TestPeerFanout` (13): constant-mode regression, degree hub > leaf, `preserve` mean-preservation on a regular graph, `additive` volume growth, kmin/kmax clamps, never-exceeds-degree, betweenness-requires-network, invalid mode/budget raise, memoisation, end-to-end run, and `k_peers=0` no-op in every mode. Full suite green (680 passed, 1 skipped). Infrastructure only — the actual hub-targeting re-run is deferred; social-scientist docs added to [docs/Simulation_Configuration_Guide.md](docs/Simulation_Configuration_Guide.md) and the flag reference in [docs/AIRE_Quickstart.md](docs/AIRE_Quickstart.md).
- [2026-07-05] v0.9 — Per-side reach targeting: persuadable + network-centrality. The reach lever (`reach_a`/`reach_b`, the audience *fraction* a broadcast reaches) previously always subsampled that audience uniformly at random. `SurveyedNation.apply_reach_subsample` in [src/cag/abm/environment.py](src/cag/abm/environment.py) now takes per-side `targeting_a` / `targeting_b` ∈ `{"random", "persuadable", "degree", "betweenness"}`, modelling *microtargeting* — spending a limited reach budget on the audience members most worth reaching rather than a random slice. `persuadable` keeps the most *undecided* (smallest `|get_real_package_index()|`, i.e. closest to the neutral midpoint); `degree` / `betweenness` keep the most *central* on the peer graph (the hubs / brokers who best amplify a message through peer messaging). All non-random modes hard-rank and keep the top `floor(reach·n)`, with a seeded permutation (same seed as the random arm) breaking ties so the arms differ only by criterion, and all are a no-op at `reach=1.0`. Centrality is a property of the peer graph, so `run_simulation` setup in [src/cag/abm/sim.py](src/cag/abm/sim.py) was reordered to build (and repair) the network *before* the reach subsample; the `random`/`persuadable` arms and the shared `sim_step` counter are provably unaffected (full suite green through the reorder). New `SIM_CONFIG` keys `reach_targeting_a`/`reach_targeting_b` (default `"random"`), `--reach-targeting-a/-b` CLI flags, both hard resume keys. `TestReachTargeting` (10) + `TestReachTargetingCLI` (2). Verified on gpt-5-mini (NB 42): persuadable kept mean |GT| 0.89 vs 2.04, degree kept mean degree 9.17 vs 3.67 — the two modes demonstrably pick different audiences.
- [2026-07-05] v0.9 — Observability layer, new diagnostic figures, and the AIRE readiness-race fix. To make the new microtargeting runs analysable — and to answer the maintainer's recurring "what exactly did each agent see?" — a full prompt-capture layer landed. A `SurveyedCitizen._chat()` wrapper in [src/cag/abm/agent.py](src/cag/abm/agent.py) records the exact `(system_prompt, user_prompt, response)` triple for every persona-facing LLM call into `agent_prompts.csv`, gated to a stratified sample (one agent per exposure bucket) so the file stays bounded no matter how many citizens the run has; the same sample drives `agent_timeline`. Per-agent `reached_by_a`/`reached_by_b` flags now record the *actual* post-subsample audience (structural exposure bucket ≠ who was reached under `reach<1` + targeting), and two new tables — `bucket_summary.csv` (one row per run × bucket, designed as the cross-run aggregation seed) and `targeting_diagnostics.csv` (reached-vs-dropped mean |GT| per side) — are built by `build_bucket_summary` / `build_targeting_diagnostics` in [src/cag/io/aggregators.py](src/cag/io/aggregators.py). The heavy `survey_assembled_context.csv` (written for every agent though only the sampled rows are consumed) was gated to the same sample at collection time. [src/cag/io/plots.py](src/cag/io/plots.py) gained eight figures (per-bucket opinion trajectories, dispersion/polarization, drift-from-GT, an opinion-distribution ridgeline, a before/after network coloured by opinion with reached nodes ringed, a two-step-flow targeting-mechanism panel, a reach-QC bar, and calibration before/after), and the trajectory plots now render Day 0 as a distinct anchor marker (conditional on the anchor mode) with CI ribbons, markers, and per-bucket GT references; a str-cast fixed an id-type mismatch that had greyed out the network figure. Separately, [scripts/aire/run.sh](scripts/aire/run.sh)'s readiness gate was switched from `/v1/models` to a real chat-completion probe, closing a vLLM-registration race that had crashed node-shared sweep jobs. Verification notebooks [notebooks/42_reach_targeting_smoke.ipynb](notebooks/42_reach_targeting_smoke.ipynb) and [notebooks/43_observability_gallery.ipynb](notebooks/43_observability_gallery.ipynb) added; write-up in [docs/result_report.md](docs/result_report.md). Suite **668 passed, 1 skipped**.
- [2026-07-03] v0.9 — Tier-P persona-null ablation (algorithmic-fidelity manipulation check). Can we prove the model actually conditions on each agent's assigned persona, rather than answering from a generic pro-climate prior? New `SurveyedNation.apply_persona_mode(mode, seed)` in [src/cag/abm/environment.py](src/cag/abm/environment.py) rewrites every active citizen's persona under one of three arms — `"real"` (no-op, bit-for-bit identical to prior canon), `"shuffled"` (each agent is given *another* agent's whole persona verbatim via a derangement, so no agent keeps its own but every persona is still a coherent real UK person), and `"neutral"` (every persona replaced by the generic `NEUTRAL_PERSONA_TEXT = "I am an adult living in the United Kingdom."`). Ground truth is **never** touched — real opinions are always read from `original_survey_data`, so the scoring target is identical across all three arms and accuracy differences isolate persona-conditioning. Personas are snapshotted *before* any override is written so the shuffle copies originals, not already-overridden strings. Mechanism in [src/cag/abm/agent.py](src/cag/abm/agent.py): new `SurveyedCitizen.persona_override` attribute + `get_persona()` hook that returns the override when set. Wiring in [src/cag/abm/sim.py](src/cag/abm/sim.py): new `SIM_CONFIG["persona_mode"] = "real"` (34 keys total), `VALID_PERSONA_MODES = ("real", "shuffled", "neutral")`, validated in `_resolve_runtime`, applied in `run_simulation` alongside the other audience manipulations (reach / audience-cap) so it runs on both fresh and resume paths; the returned map is stored as `nation.persona_map`. Audit trail: new `persona_map.csv` (`agent_id, source_agent_id, persona_mode`) via `_RESULT_CSV_SCHEMAS["persona_map"]` in [src/cag/io/results.py](src/cag/io/results.py) (final-output only, in `_CHECKPOINT_SKIP_KEYS`). `persona_mode` added to `_RESUME_HARD_KEYS` in [src/cag/io/checkpoint.py](src/cag/io/checkpoint.py) (changing the arm mid-run would invalidate the comparison). New `--persona-mode {real,shuffled,neutral}` CLI flag in [src/cag/__main__.py](src/cag/__main__.py); new `tierP` bundle in [src/cag/presets.py](src/cag/presets.py) (100 agents, single empty-phase Day-0-only day, package mode, `day0_anchor="llm_survey"` so each Day-0 opinion is a pure function of persona + policy, `memory="persona_only"` to strip cross-policy leakage). New sweep [scripts/aire/sweeps/tierP.txt](scripts/aire/sweeps/tierP.txt) (3 arms × seeds 42/43/44). New [tests/test_persona_mode.py](tests/test_persona_mode.py) (20 tests: override hook, all three modes, derangement property, GT-untouched invariant, map shape, CLI + resume plumbing). The full spec is [docs/Model_Design.md](docs/Model_Design.md) §31. NB for the persona-null demo is deferred until AIRE run directories return.
- [2026-07-03] v0.9 — Broadcast-frequency-asymmetry CLI. The per-day `make_phases` shorthand (v0.5 §19) could already make one side broadcast more often than the other, but only by hand-writing a `days` list. Three new flags in [src/cag/__main__.py](src/cag/__main__.py) apply the same asymmetry to *every* day when `--days` is a plain integer: `--broadcasts-a N` / `--broadcasts-b M` (default 1/1) set each politician's daily broadcast count, and `--interleave` / `--no-interleave` (default on) chooses between alternating (`A,B,A,…`) and block (`A,A,…,B`) ordering. New `build_days(n_days, broadcasts_a=1, broadcasts_b=1, interleave=True)` loops the days through `make_phases(..., a_first=(i % 2 == 0))` so the alternation-of-first-speaker convention is preserved; `build_config` pops the three knobs from the merged dict, expands an integer `days`, and warns if they are set while `days` is already an explicit list (in which case they are ignored). Peer chat (`C`) is always appended — disable with `--k-peers 0`. The default 1-vs-1 reproduces the classic schedule exactly. This is a **frequency** lever (how *often* a side speaks), orthogonal to `reach_a`/`reach_b` (what *fraction of the audience* a single broadcast reaches). New `TestBroadcastFrequency` class in [tests/test_cli.py](tests/test_cli.py) (9 tests: symmetric-default unchanged, block vs interleave expansion, knob-popping, flag parsing, absent-when-unset, dry-run). Documented in [docs/Simulation_Configuration_Guide.md](docs/Simulation_Configuration_Guide.md) ("Broadcast-frequency asymmetry from the command line") and [docs/Model_Design.md](docs/Model_Design.md) §32.
- [2026-07-03] v0.9 — Affinity-weight presets simplified to an explainable three-tier ladder, and AIRE run labels. The `balanced` default (`DEFAULT_AFFINITY_WEIGHTS` in [src/cag/abm/config/exposure.py](src/cag/abm/config/exposure.py)) is now a clean factor-2 ladder: political signals (`brexit`, `politics`, `vote_bonus`) = 2.0, values (`openness`, `selftransc`, `conformtrad`, `sdo`, `rwa`) = 1.0, demographics (`age`, `education`, `region`) = 0.5 — anchored to Fletcher & Nielsen (2017) on partisan selective exposure and Steg & de Groot (2010) on value-based environmental attitudes. The two sweep presets keep the same ladder with one tier pushed: `vote_dominant` raises the political tier 2.0 → 4.0; `values_dominant` swaps the top two tiers (values 4.0, political 1.0, demographics 0.5). Because the affinity-rank algorithm uses scores only to *order* citizens before slicing to the target cell shares, the weights change *who* lands in each audience cell but never the cell sizes. Caveat recorded: NB 27's reported preset-agreement percentages predate this simplification and no longer match the shipped presets; NB 27 is left as a historical v0.5 pilot and not re-run. Separately, [scripts/aire/run.sh](scripts/aire/run.sh) now honours an optional `RUN_LABEL` env var, appended as a suffix to the scratch output directory (`run_${SLURM_JOB_ID}${RUN_LABEL:+_${RUN_LABEL}}`) with a banner echo, so sweep jobs land in self-describing folders (`RUN_LABEL=split50_freq3v1 sbatch scripts/aire/run.sh …`). Researcher-facing walkthrough of the affinity scorecard/sort-and-slice mechanism and the new weight table added to [docs/Simulation_Configuration_Guide.md](docs/Simulation_Configuration_Guide.md); write-up in [docs/Model_Design.md](docs/Model_Design.md) §33. Full v0.9 suite: **647 passed, 1 skipped** (was 610 at the end of v0.8).
- [2026-07-01] v0.8 — Configurable memory / prompt assembly. The hard-wired v2 tiered-memory pipeline is now declaratively configurable so social scientists handed the repo can run memory-ablation experiments (does the Day-0 anchor cause opinion lock-in? does re-injecting own prior reasoning? does the verbatim window length matter?) without editing `agent.py`. New subpackage [src/cag/abm/config/](src/cag/abm/config/) with the shared `DEFAULT_* / *_PRESETS / resolve_* / validate_*` shape: [config/exposure.py](src/cag/abm/config/exposure.py) **relocates** the exposure target/weight vocabularies out of [environment.py](src/cag/abm/environment.py) (which now re-exports every public name with `# noqa: F401`, so `from cag.abm.environment import TARGET_PRESETS` etc. still resolve — this un-buries the exposure config the maintainer had flagged as awkwardly mixed into the environment module), and [config/memory.py](src/cag/abm/config/memory.py) holds the new memory schema. Six context sections (persona, Day-0 anchor, daily summaries, recent reflections, own reasoning, today-so-far) are individually toggleable, plus an off-by-default `opinion_trajectory` numeric block. A single `verbatim_window_days` knob (default 2) is the one source of truth for the previously-scattered "last 2 days" constant that appeared in §3/§4/§5 assembly and in `manage_memory` compression — three helpers (`verbatim_days`, `summary_upper_exclusive`, `compression_target_day`) derive all four call sites from it, with a property test proving the verbatim and summarised day-sets partition days `1…day` with no gap or overlap. `day0_anchor.ttl_days` retires the anchor after N days; per-stage overrides (`peer_message` / `reflection` / `survey`) let a section differ by prompt stage. Presets: `default`, `short_memory`, `wide_memory`, `no_compression`, `no_anchor`, `anchor_ttl2`, `no_own_reasoning`, `reflections_only`, `persona_only`. Wiring: new `SIM_CONFIG["memory"] = "default"` key (33 keys total) resolved + validated in `_resolve_runtime` and deep-copied onto every agent in `run()` ([sim.py](src/cag/abm/sim.py)); `"memory"` added to `_RESUME_HARD_KEYS` ([io/checkpoint.py](src/cag/io/checkpoint.py)) because the window determines which days are compressed into stored summaries; new `--memory` CLI flag (preset name or JSON dict) in [__main__.py](src/cag/__main__.py). `assemble_context` / `get_system_prompt` gained a `stage` parameter threaded through every citizen-side call site in [agent.py](src/cag/abm/agent.py). **The default config reproduces the §28 behaviour bit-for-bit**, guarded by the unchanged golden portion of [tests/test_memory.py](tests/test_memory.py); one subtlety surfaced and was fixed during the build — `verbatim_days` must include day 0 (recent reflections keep a Day-0 reflection if it is in-window) while own-reasoning keeps its own `d>0` filter (Day-0 reasoning lives in the anchor). New tests: [tests/test_memory_config.py](tests/test_memory_config.py) (schema/presets/validation/window-helper partition property, ~38 tests) and `TestMemoryConfigToggles` in test_memory.py (every non-default toggle, 13 tests), plus `memory` resolution/resume/CLI cases in test_sim.py & test_cli.py. Offline demo notebook [notebooks/35_memory_ablation_demo.ipynb](notebooks/35_memory_ablation_demo.ipynb) shows one agent's assembled context changing across presets with no LLM calls. Suite **610 passed, 1 skipped** (was 552). No `__version__` bump. Full write-up in [docs/Model_Design.md](docs/Model_Design.md) §30; [USER_GUIDE.md](USER_GUIDE.md) gained a v0.8 configuration-path section.
- [2026-07-01] v0.8 — `debias` option removed; the two-step Condition B survey is now unconditional. It had been research canon since v0.3 and the simulation loop always ran it (including the `llm_survey` Day-0 anchor), so the non-debias single-call path was dead code. In [src/cag/abm/agent.py](src/cag/abm/agent.py) the dead `get_user_prompt()` helper and the `if debias / else` branch in `administer_survey()` were deleted (Step-1 anti-sycophancy reasoning + Step-2 answer now always run), and the `debias` parameter was dropped from `administer_survey()` and `run_baseline()`. The removal was threaded through `run_end_of_day_survey()` in [src/cag/abm/environment.py](src/cag/abm/environment.py) and [src/cag/abm/sim.py](src/cag/abm/sim.py) (the `SIM_CONFIG["debias"]` key — now 32 keys — the `_run_baseline_surveys` / `_run_day0` parameters, the resolved runtime dict, the package + single-policy EOD call sites, the two Day-0 call sites, the "debias flag is ignored on Day 0" INFO log, and the `debias` field of the `[llm]` config-log line), plus the `--debias` CLI flag in [src/cag/__main__.py](src/cag/__main__.py), the `debias` key in both `RUN_BUNDLE_PRESETS` bundles in [src/cag/presets.py](src/cag/presets.py), and `debias` in `_RESUME_SOFT_KEYS` in [src/cag/io/checkpoint.py](src/cag/io/checkpoint.py) (checkpoint-safe: old checkpoints carrying a `debias` key are simply never compared). The `_DEBIAS_STEP1_TEMPLATE` / `_DEBIAS_STEP2_TEMPLATE` prompts and the `survey_reasoning` "debias trace" are retained — they are the two-step mechanism itself, now non-optional. Trade-off accepted by the maintainer: `run_baseline()` can no longer produce a raw, un-debiased survey (the NB 11–14 style bias-measurement path), since it now always runs the two-step chain. The `thinking` flag — also always `False` in canon — was deliberately left in place because it is a live research toggle exercised with `True` in NB 24/25/34. Tests trimmed accordingly (`TestDebiasedSurvey` non-debias cases, CLI debias assertions, `test_debias_default_true`, `test_debias_with_anchored_mode_logs_override`); docs updated ([docs/Simulation_Configuration_Guide.md](docs/Simulation_Configuration_Guide.md) `debias` row, section, and resume note removed).
- [2026-06-28] v0.8 — Day-0 anchor cross-step consistency fix. In package mode the Day-0 identity anchor (memory section 2) was present only at end-of-day survey time but absent during peer-message generation and reflection, because those steps call `assemble_context` with `policy_id=PACKAGE_SCOPE` and no `target_policy_id` (every target-scoped section empty). The agent effectively "forgot" its verbatim Day-0 position between steps and regained it at survey time — poor cognitive modelling and a validity concern (peer messages came from a thinner self-model than survey answers). New `_section_day0_anchor_all()` in [src/cag/abm/agent.py](src/cag/abm/agent.py) emits every policy's Day-0 rationale in canonical `SURVEY_QUESTIONS` order under `Original prior positions:`; `assemble_context` now uses the focused single-policy `_section_day0_anchor()` when there is a real `target_policy_id` (survey, unchanged — the survey stays focused on the policy being asked) and the all-policy form for package-scoped steps with no single target (peer/reflection). **Only section 2 is propagated** — section 5 (recent considered position) is redundant with summaries+reflections in a holistic step and stays survey/target-scoped, and section 6 (today's answers-so-far) is chronologically not yet taken at peer-message time and stays survey-only. Single-policy mode was already consistent and is unchanged; the survey's single anchor is a subset of the package form the agent saw earlier in the day, so nothing is forgotten peer→survey. Tests: `TestDay0AnchorSection` swaps `test_anchor_omitted_when_target_is_package_scope` for `test_package_anchor_lists_all_policies_when_no_single_target` and `test_package_anchor_present_on_peer_reflection_path`; suite **557 passed, 1 skipped**. Full write-up in [docs/Model_Design.md](docs/Model_Design.md) §20.7; [docs/Prompts_and_Personas_Guide_v2.md](docs/Prompts_and_Personas_Guide_v2.md) §2.2 documents the two anchor forms.
- [2026-06-23] v0.8 — v2 tiered memory architecture and Day-0 anchor compression removed. [src/cag/abm/agent.py](src/cag/abm/agent.py) `assemble_context()` rewritten as an ordered six-section build (§1 persona + values, §2 Day-0 anchor — *verbatim from `survey_reasoning[target_policy_id]`*, §3 `daily_summaries` for days `<d-1`, §4 reflections for days `d-1, d`, §5 own reasoning for days `d-1, d` scoped to `target_policy_id`, §6 package-mode-only today-so-far block). New API split: `policy_id` filters §3 + §4 (context scope), `target_policy_id` scopes §2 + §5 + §6 (question scope) — in package mode the EOD survey passes `context_policy_id=PACKAGE_SCOPE` (cross-policy reflections survive) but `target_policy_id=policy_id` (anchor and own-reasoning narrowed to the policy being asked). `agent.day0_anchors` dict, `compress_day0_anchor()` method, post-`_run_day0` compression loop in `sim.py`, `day0_anchors.csv` schema entry in `_RESULT_CSV_SCHEMAS`, and the corresponding checkpoint hydration block in `io/checkpoint.py` all **removed**: one fewer LLM call per agent at Day 0, and §2 is now provably the same string the agent wrote during the Day-0 rationale step. `manage_memory(day, policy_id, …)` now runs once per agent per day before the EOD survey, compressing day `d-2` into `daily_summaries[(d-2, policy_id)]`. Tests: `TestCompressDay0Anchor` removed; `TestDay0AnchorSection`, `TestSectionOrder`, `TestSectionOrder.test_full_package_mode_section_order` rewritten for the verbatim/target-scoped path. Suite **556 passed, 1 skipped, 21 subtests passed** (+18 from new section-order coverage). v0.7 checkpoints resume cleanly into v0.8: `_RESUME_HARD_KEYS` / `_RESUME_SOFT_KEYS` unchanged, §2 anchor rebuilt from `survey_reasoning` at the first `assemble_context()` call. The `day0_anchor` SIM_CONFIG key (singular, anchor-*mode* selector) is unchanged. Full write-up in [docs/Model_Design.md](docs/Model_Design.md) §28.
- [2026-06-23] v0.8 — Operational cleanups and researcher-onboarding doc. Dead `SIM_CONFIG["output_dir"]` key removed from [src/cag/abm/sim.py](src/cag/abm/sim.py) (`SIM_CONFIG` now 33 keys; `__main__.py` reads `--outdir` and passes directly to `save_results`); the `[peer]` line of `_log_experiment_config()` now renders the resolved `_resolve_network_params(cfg)` dict as `params={k=v, …}` so non-SBM topologies (Watts–Strogatz, Barabási–Albert, Erdős–Rényi, homophily-weighted) report their actual parameters instead of misleading SBM-only `p_intra` / `p_inter`. [docs/AIRE_Quickstart.md](docs/AIRE_Quickstart.md) §6 gained a `> **Important — first-time model download.**` blockquote recommending `--time=06:00:00` on first submission with a fresh model (the 1500 s vLLM-readiness wait baked into `scripts/aire/run.sh` can be exceeded on the first cold-cache HuggingFace download). New [docs/Code_Tour.md](docs/Code_Tour.md) — ~25-page newcomer walkthrough of `src/cag/` covering audience + conventions, 30-minute skim sequence, 15 file-by-file walkthroughs (medium depth on `sim.py` + `agent.py` with annotated excerpts), two side-trips (political-exposure affinity-rank mechanism, v2 memory annotated example), 6-recipe cookbook, 11-term glossary, 10-step AIRE pre-flight checklist. NB 34 v2-memory smoke ([notebooks/34_memory_v2_smoke.ipynb](notebooks/34_memory_v2_smoke.ipynb)) added; configured for local Qwen3-8B-4bit, local Apple-Silicon throughput too low for Day-0 in reasonable time so AIRE re-run is the planned path. Full write-up in [docs/Model_Design.md](docs/Model_Design.md) §29.
- [2026-06-23] v0.8 — `sim.py` modular split. [src/cag/abm/sim.py](src/cag/abm/sim.py) reduced from 2755 → 838 lines (orchestration only: `run_simulation`, `_run_one_day`, `_resolve_runtime`, `SIM_CONFIG`); five focused modules extracted: [src/cag/abm/network_repair.py](src/cag/abm/network_repair.py) (263 lines, v0.7 3-layer connectivity defence), [src/cag/io/aggregators.py](src/cag/io/aggregators.py) (506 lines, `_collect_results` post-processing builders), [src/cag/io/plots.py](src/cag/io/plots.py) (555 lines, `save_result_plots` + every per-figure plotter), [src/cag/io/results.py](src/cag/io/results.py) (512 lines, `save_results` + CSV schemas + JSON serialisation), [src/cag/io/checkpoint.py](src/cag/io/checkpoint.py) (366 lines, save/resume primitives + `sim_step` rehydration). **Pure structural move, zero behaviour change.** Validated end-to-end against the pre-refactor NB 32 baseline: all deterministic outputs (`config.json`, `ground_truth.csv`, `package_ground_truth.csv`, `agent_attributes.csv`, `network_snapshot.json`) bit-identical; all 22 LLM-driven CSVs have identical schemas and row counts; numeric distributional stats match within gpt-5-mini @ T=0.5 stochasticity. Test suite **538 passed, 1 skipped** (unchanged); no test rewrites required, imports updated to new module paths. No `__version__` bump — v0.8 is a refactor-only label that will hold all subsequent refactor work this cycle.
- [2026-06-22] v0.7 — Outputs expansion (17 → 29 artefacts) + network 3-layer connectivity defence landed and validated by NB 32. `collect_agent_attributes` persists `(agent_id, bucket, _affinity_score_a, _affinity_score_b, demographic IDs, persona text)` per agent. Five new builders join the agent-attributes table to per-day signals: `build_package_index_by_bucket`, `build_opinion_shares_by_bucket`, `build_day0_vs_dayN_shifts`, `build_calibration_table`, `build_message_flow`. `_safe_network_snapshot` writes a JSON-safe nodes/edges payload; `plot_network_graph`, `plot_package_index_by_bucket`, `plot_opinion_shares_by_bucket`, `plot_gap_widening`, `plot_calibration_by_policy` wired through `save_result_plots`. New `survey_assembled_context` capture on `SurveyedCitizen` records the **exact ~1–18 KB system prompt** every agent saw at every survey call. `build_agent_timeline` returns a long-format DataFrame with 9 event types stratified-sampled across the top-3 exposure buckets; final-output only via `_CHECKPOINT_SKIP_KEYS`. Monotonic `nation._sim_step` counter (lazy-init, resume-safe) wired through every event-logging site; new `sim_step` column on `messages`, `reflections`, `survey_reasoning`, `survey_raw_response`, `survey_assembled_context`, and `daily_summaries`. Network defence: SBM `p_inter` default 0.02 → 0.05 (Bakshy 2015, Halberstam-Knight 2016 ~25% cross-cutting), ER `p` 0.05 → 0.10, plus `_adjust_network_params_for_small_n` (Layer 1, adaptive bump for n<100), `_auto_connect_components` (Layer 2, deterministic auto-repair via `(k-1)` bridging edges from each smaller component to the largest), `_log_network_summary` (Layer 3, visibility) and a new `auto_connected_edges` field in `network_diagnostics.json`. New [tests/test_timeline_and_outputs.py](tests/test_timeline_and_outputs.py) (30 tests) + 13 connectivity tests in [tests/test_networks.py](tests/test_networks.py); suite now **538 passed / 1 skipped** (was 432). `__version__` bulk-bumped to 0.7.0 across 18 source modules — retires the long-standing 0.2.0 / 0.3.0 / 0.5.0 / 0.6.0 / 1.0.0 inconsistency. Full docs sweep: CHANGE_LOG [0.7], ROADMAP §0.7, this entry, Model_Design §22–§26, USER_GUIDE v0.7 path, README status. Deep rewrites of [docs/Run_Output_Guide.md](docs/Run_Output_Guide.md) and [docs/Simulation_Configuration_Guide.md](docs/Simulation_Configuration_Guide.md) deferred to a follow-up session.
- [2026-06-21] v0.7 — NB-31 package-mode survey-context fix (silent bug since v0.4): `administer_survey()` and `run_end_of_day_survey()` now pass `PACKAGE_SCOPE` to `assemble_context()` in package mode, so the in-context memory is the union over all six climate policies instead of the empty per-policy slice. Discovery: NB 30 surgical replay across models. Validation: NB 31 fix-replay confirmed amplified cross-bucket gap. Production confirmation: **Run-14 v2 measured +0.667 Day-5 cross-bucket gap-widening (vs +0.053 pre-fix) — a 12.6× amplification** matching the GPT-5.4-mini surgical-replay prediction within noise (full write-up in [docs/result_report.md](docs/result_report.md)). Also landed: `k_peers=0` short-circuit in `run_peer_messaging` / `run_package_peer_messaging` (saves ~240 LLM calls per r14-style broadcast-only run), per-day checkpointing CLI default-on (`argparse.BooleanOptionalAction, default=True` on `--checkpoint-every-day`; Python-side `run_simulation()` default still `False` for interactive notebooks), full CLI argparse refactor in `src/cag/__main__.py` (every `SIM_CONFIG` knob exposed; `--preset` / `--list-presets` / `--dry-run` composition layer), preset bundle registry in new `src/cag/presets.py` (`RUN_BUNDLE_PRESETS = {"smoke", "r14_canonical"}`), and AIRE / HPC infrastructure ([scripts/aire/run.sh](scripts/aire/run.sh) thin sbatch launcher, [scripts/aire/sweep.sh](scripts/aire/sweep.sh) multi-job submitter, [scripts/aire/sweeps/r14_v2.txt](scripts/aire/sweeps/r14_v2.txt) canonical 3-condition sweep, [docs/AIRE_HPC_repo_primer.md](docs/AIRE_HPC_repo_primer.md) and [docs/AIRE_Quickstart.md](docs/AIRE_Quickstart.md) walkthrough). vLLM auto-start with `$HF_MODEL` overrideable. Carried `_safe_int` v0.5 fix forward; added `isinstance(G, nx.Graph)` guards on Layer 2/3 connectivity helpers so MagicMock-based unit tests pass cleanly.
- [2026-05-24] v0.6 — Canonical defaults consolidated and documented. `SIM_CONFIG` in `src/cag/abm/sim.py` now reflects the research-canon profile (`n_citizens=100`, package-mode alternating day order, `llm_provider="local"`, `llm_model="mlx-community/Qwen3-8B-4bit"`, `debias=True`, `day0_anchor="ground_truth_with_rationale"`, `political_message_source="offline"`, `political_message_set="v1"`).
- [2026-05-24] v0.6 — Offline political-message path promoted to first-class runtime mode with strict startup validation and traceable broadcast provenance (`political_message_id` in `messages.csv`). Resume compatibility surface expanded to hard-fail on `political_message_source` / `political_message_set` drift.
- [2026-05-24] v0.6 — New canonical operator/supervisor documentation and smoke notebooks landed: [Simulation_Configuration_Guide.md](docs/Simulation_Configuration_Guide.md), [notebooks/28_offline_political_messages_smoke.ipynb](notebooks/28_offline_political_messages_smoke.ipynb), [notebooks/29_canonical_full_smoke.ipynb](notebooks/29_canonical_full_smoke.ipynb). NB29 validated the canonical full-stack path; checkpoint/resume remained validated by NB18 (not re-exercised in NB29).
- [2026-05-20] v0.5 — Political-agent prompt de-identification. `_DEFAULT_PRO_CLIMATE_PROMPT` and `_DEFAULT_ANTI_CLIMATE_PROMPT` in `src/cag/abm/agent.py` reframed as a generic *fallback* path: all party and politician names removed (Green Party of England and Wales, Reform UK, Zack Polanski, Caroline Lucas, Nigel Farage, Richard Tice), non-climate spillover trimmed (water companies, NHS, free public transport, the "For the Common Good" slogan, wealth-tax-on-the-super-rich content), target audience narrowed to climate-relevant cohorts. `_ANTI_SYCOPHANCY` shortened (dropped the over-leading "Real people like you hold a WIDE range of views… That is expected and acceptable" sentence); `_DEBIAS_STEP1_TEMPLATE` updated to reference "messages and reflections from today and previous days" so wording matches the v0.5.1 multi-day daily-context surveys. [Prompts_and_Personas_Guide_v2.md](docs/Prompts_and_Personas_Guide_v2.md) status banner regenerated as fallback framing; §3.1 / §3.2 / debias step-1 verbatim quotes refreshed. Motivation: keep the v0.5 paper's political-agent layer institution-agnostic (the briefs are a fallback for users without a real-message corpus); the next-step direction is ingesting real-world political messages (party press releases, MP speeches, campaign material) rather than LLM-generated party-style messages.
- [2026-05-20] v0.5 — Affinity-based political-exposure sanity check (NB 27) and two bug fixes. `notebooks/27_affinity_exposure_demo.ipynb` validates the committed-minority audience reframe on the full YouGov pool (N = 1483): `rule_affinity_rank` hits both shipped target presets to within rounding, all four pre-registered validation gates pass, and the three weight presets produce non-trivial cell-level disagreement. Bug 1: `_safe_int` silently zeroed every `GABMAttributeID` ordinal (psych-scale signals were dead on every `build_nation` citizen); rewritten to read `.id` first, new `TestSafeInt` regression class closes the unit-test gap. Bug 2: `rule_affinity_logistic` removed entirely — independent Bernoulli on anti-correlated A/B scores (`corr = -0.975`) missed cell-share targets by ±18 pp despite hitting per-side marginals; v0.5 now ships with two modes only (`rule_priority_chain`, `rule_affinity_rank`). Suite at **432 passing, 1 skipped** (430 → 432). Full write-ups in [docs/result_report.md](docs/result_report.md), [docs/Model_Design.md](docs/Model_Design.md) §18.15, [docs/Literature_Political_Exposure.md](docs/Literature_Political_Exposure.md) §6 / §6.3 / §7.
- [2026-05-16] v0.5 — Prompt audit and unification. Every citizen-side LLM prompt in `src/cag/abm/agent.py` brought into consistent first-person framing (debias chain and Day-0 rationale prompts flipped from 2P/3P mix); end-of-day surveys (vanilla and debias Step 1) now explicitly reference the in-context memory; `get_persona()` is now the canonical merged-persona public method (old `get_persona` / `get_narrative` renamed to private `_build_demographics_text` / `_build_values_text`); end-of-context reminder line renamed to `"Remember who you are:"`; new `SURVEY_SHORT_LABELS` dict fixes the Day-0 rationale bullet truncation bug; `[P-A]/[P-B]/[C]` tags stripped from in-context reflection bullets (CSV audit trail unchanged); `compress_memories` extended from 2 → 4–5 phase-agnostic first-person sentences; single-policy `receive_peer_messages` extended for symmetry with package variant. Five existing test assertions updated; full suite **403 passing, 1 skipped** (unchanged). New canonical doc [Prompts_and_Personas_Guide_v2.md](docs/Prompts_and_Personas_Guide_v2.md) (verbatim prompt quotes, real worked example from [`data/output/experiments/20260425_082317/`](data/output/experiments/20260425_082317/)); V1 guide preserved with superseded banner. v0.5 prompt-overhaul section appended to [Model_Design.md](docs/Model_Design.md) (§20). Condition B bias-reduction re-measurement on the new 1P debias chain deferred.
- [2026-05-09] v0.5 — Local-LLM provider (`provider="local"`) added to `cag.io.llm`: substring-keyed `_MODEL_REGISTRY` (Qwen3 / Llama / Apertus / Mistral / DeepSeek-R1), `configure_local()` / `ping_local()` startup health-check, empty-thinking-truncation retry guard, `load_api_key("local")` short-circuit. Three new `SIM_CONFIG` keys (`local_base_url`, `local_extra_body`, `local_timeout_s`) wired through `_resolve_runtime` and added to `_RESUME_SOFT_KEYS`. NB 24 (monkey-patched smoke) and NB 25 (integrated parity, bit-for-bit identical metrics) added. Full V3 rewrite of [Local_LLM_Setup_Guide.md](docs/Local_LLM_Setup_Guide.md) covering mlx-lm / Ollama / vLLM / sglang / llama.cpp; new [requirements-local.txt](requirements-local.txt). Suite at 365 passing, 1 skipped (up from 339).
- [2026-04-23] v0.4 initial release work — Package communication mode, Day-0 ground-truth anchoring (3 modes: `llm_survey` / `ground_truth` / `ground_truth_with_rationale`), and the memory anchor refactor (numeric Day-0..N trajectory replaced with a Day-0 rationale block in `assemble_context()`) landed alongside notebooks 16 and 17.
- [2026-04-24] v0.4 — Checkpoint and resume added to `run_simulation()` (`checkpoint_dir`, `resume`, `checkpoint_every_day`), notebook 18 added, suite extended to 328 tests.
- [2026-04-25] v0.4 — Production package-mode run documented in NB 19; reach-control tooling added (`reach_a`, `reach_b`, `apply_reach_subsample()`), then mirrored with `audience_cap` / `apply_audience_cap()` and broadcast-only validation notebooks 20 and 21.
- [2026-04-26] v0.4 — Day-0 survey-path audit and persona-signal evaluation notebooks 22 and 23 added; `docs/result_report.md` extended through Runs 5–8 and calibration analyses; current baseline is 340 collected tests (339 passing, 1 skipped) across 15 test files.
- [2026-04-19] v0.3 — Bias calibration complete. Condition B debias integrated, Anthropic provider, ground truth utility, 276 tests, 4 experiment runs. See [result_report.md](docs/result_report.md).
- [2026-04-14] Baseline bias investigation: 6-model comparison, 4-condition experiment, multi-policy generalization (NB 11–14).
- [2026-04-10] v0.2 MVP complete — all 10 issues done, 226 tests, 8 demo notebooks (01–08).
- [2026-04-02] 10-issue implementation sprint begun (Issues 1–10 from [github_issues.md](docs/github_issues.md)).
- [2026-03-30] Dependency update to [GABM==0.2.18](https://pypi.org/project/gabm/0.2.18/).
- [2026-03-12] Dependency update to [GABM==0.2.17](https://pypi.org/project/gabm/0.2.17/).
- [2026-03-05] Dependency update to [GABM==0.2.16](https://pypi.org/project/gabm/0.2.16/).
- [2026-02-20] Dependency update to [GABM==0.2.6](https://pypi.org/project/gabm/0.2.6/).
- [2026-02-18] Dependency update to [GABM==0.2.2](https://pypi.org/project/gabm/0.2.2/).
- [2026-02-16] Planned release of Climate-Action-GABM==0.1.0.
- [2026-02-10] Documentation and Makefile added based on those developed for [GABM](https://github.com/compolis/GABM).
- [2026-02-03] Andy added as developer/maintainer. BSD License agreed and put in place.
- [2026-02-02] Climate-Action-GABM Repository transered to [compolis](https://github.com/compolis/)
- ...